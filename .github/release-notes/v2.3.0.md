## PatchViewer v2.3.0

### 中文

重写了核心数据库 `stats` 的渲染模式，使补丁前后差异显示更贴合原版数据库布局。

### English

Rewrote the core database `stats` rendering mode so before/after patch differences follow the native database layout more closely.
